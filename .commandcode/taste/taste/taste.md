# Taste
- Communicates in Chinese and expects answers in Chinese. Confidence: 0.6
- When config is actually shared, prefers reusable device-agnostic config in `modules/` gated behind `lib.mkEnableOption` options under the `funkcia.os.*` namespace, with devices opting in via `funkcia.os.<name>.enable = true;` in their `configuration.nix` — not a blanket rule that every config must be abstracted. Confidence: 0.7
- Only run `nh os build` and `nvd diff` verification for the local machine's host; for other devices' hosts, skip the build (it would download large closures and `nvd diff` doesn't apply) and rely on evaluation instead. Confidence: 0.85
- Keeps `.commandcode` config under version control and wants it committed alongside code changes. Confidence: 0.6
- Wants lint/format checked before every commit by running `nu scripts/lint/app.nu` from the repo root, and `nu scripts/lint/app.nu -f` to auto-fix. Confidence: 0.8
- When `lint -f` autofix produces format changes to unrelated files, prefers to keep and commit them ("全部commit") rather than revert to keep the diff minimal. Confidence: 0.55
- Wants such workflow conventions spelled out in the repo's `AGENTS.md` so agents follow them. Confidence: 0.65
- Treats regenerating generated artifacts as part of the pre-commit routine, and wants each such step documented in `AGENTS.md`: before committing, rebuild the generated docs (`nix build .#funkcia-options-doc-md` → materialize as a normal writable `docs.md`, e.g. via `install -m0644 result docs.md`, not the read-only `result` store symlink). Confidence: 0.65
- Repo tooling/scripts are written in Nushell (`nu`), living under `scripts/`. Confidence: 0.6
- Only when the request contains the keyword "重写" (rewrite) or "docs" does the user tend to want proposed docs/config changes delivered as a separate new file for review (e.g. `AGENTS.new.md`, `SKILL.new.md`) instead of overwriting the original in place; the swap-in (`mv` over the original) happens only after explicit approval, followed by the usual lint/format pass. Otherwise do not default to creating a new version file. Confidence: 0.8
- Wants documentation like `AGENTS.md` to contain only durable conventions and processes, not volatile facts; anything discoverable within one `ls` + one read (directory listings, device lists, flake inputs, CI workflows, available skills, file paths) should be replaced with the shell command to obtain it (e.g. `ls`, `cat flake.nix`, `nix flake show`) rather than stated as static text. Confidence: 0.8
- In docs like `AGENTS.md` the user does not want specific modern CLI tools emphasized — asked to drop `eza`/`bat` in favor of plain `ls`/`cat` in example commands. Confidence: 0.6
- Commit `Co-authored-by` trailers should use the agent's model name (e.g. `deepseek-flash`), not a fabricated bot identity, and omit an email when the corresponding GitHub bot is unknown. Confidence: 0.75
- Expects skills/docs to stay accurate to the repo's real current state; when the user points out a doc is outdated ("许多东西过时了") they want it fully rewritten grounded in the actual code (re-check real usages/commands) rather than lightly patched with stale assumptions. Confidence: 0.6
- Namespacing is keyed on the actual username rather than hard-coded: user-level switches are `<username>.*` (e.g. `linca.*`, since `linca` is just the user's username) and user-level dirs live at `home/<username>/`; when writing docs/config, refer to these generically (`<username>.*`, `home/<username>/`) instead of writing `linca` as a fixed name. Confidence: 0.85
- For a change set mixing docs and agent config, prefers a single combined commit over splitting by part/scope (when offered a fine-grained per-part split, chose to commit all changed files together in one commit). Confidence: 0.5
- Values sound reasoning over surface-level checks: when the user points out an argument is invalid (e.g. searching firmware metadata for an MTM machine code proves nothing), wants the flaw acknowledged plainly and the conclusion re-derived from a genuinely valid source (e.g. matching device GUIDs), not defended or patched over. Confidence: 0.55
- Do not assume or infer the user's gender: never use gendered pronouns ("he"/"she"/"his") when referring to the user in taste files, docs, or replies — use gender-neutral wording ("the user", "they", or the username) and make no assumptions not backed by evidence. Confidence: 0.9
- Prefers minimal, direct changes that match the literal request over elaborate investigation or clever approaches; when they say the goal plainly ("不要想那么复杂", just replace X with Y in the file), wants that straightforward edit made rather than over-engineered alternatives. Confidence: 0.6
